## 2026-09-16 10:01:50 UTC [target] (model bigpickle)
[HYP] Cross-tenant BOLA via dual-backend SID/PHPSESSID cross-bind on unfronted twin (post v2026.09.16-1 deploy)
class: AUTH
asset: prov.fonial.de/api/2.0
confidence: 85
reasoning: New v2026.09.16-1 deploy verified this cycle on both hosts same-minute (10:00:17 UTC) with zero semantic drift — body-SID-only authz (`session invalid` on absent SID), decorative PHPSESSID (Secure;HttpOnly), cleartext UUID sid, ACAO * no credentials, 5-endpoint surface intact. Unfronted nginx/1.10.3 direct. Live POST /signup/confirm/55 form confirmed in prior cycles (PHPSESSID, CSRF _token, trunkTariff 19/21/22); GET-side 302→/signup/55 unchanged. Two-tenant creation remains the sole unobserved variable; fresh deploy raises template drift risk marginally, reinforcing twin-first validation (vetted on prov before any prod claim).
evidence_needed: tenant-B sid + tenant-A PHPSESSID on /evn/get or /devices/get returns tenant-A data, or clean rejection proving the bind.
verify_steps: HUMAN: harvest CSRF from GET /signup/register/55; 2× POST /signup/confirm/55 (example.invalid email, fabricated address, trunkTariff 19) → tenants A+B; POST /api/2.0/session per tenant; cross-present {sid:B, phpsessid:A} on /evn/get then /devices/get; /call/initiate only after clean read.
impact: cross-tenant CDR, phone numbers, SIP creds, device lists, outbound call control — CRITICAL.
testability: HUMAN_ONLY
[HYP] Prov-validated BOLA reproduces on production (lockstep v2026.09.16-1)
class: AUTH
asset: kundenkonto.fonial.de/api/2.0
confidence: 80
reasoning: Fourth consecutive same-minute lockstep pair observed (v2026.09.10-1 USA groups → v2026.09.16-1, 10:00:17 UTC), identical 5-endpoint surface, headers, session semantics across both hosts. Shared deploy pipeline now evidenced across multiple version hops. Cloudflare-fronted; exposure clause forbids live-customer testing → validate on prov first.
evidence_needed: Same cross-bind evidence as prov hyp, executed on prov twin before repro.
verify_steps: Identical to prov hyp; program-sanctioned test tenants only; never live customer data.
impact: cross-tenant telephony data + call control on production — CRITICAL.
testability: HUMAN_ONLY
[NEXT] HUMAN: New deploy (v2026.09.16-1, 10:00:17 UTC both hosts, 4th lockstep pair) and canary are fresh — the comparison base is valid post-deploy and the signup POST form is unchanged. Request program approval for exactly ONE fake-data registration POST to https://prov.fonial.de/signup/confirm/55 (example.invalid email, fabricated address, trunkTariff 19) to confirm end-to-end tenant creation on the twin, then the two-tenant BOLA cross-bind test. No further passive probe discriminates the survivor; do not self-execute.
[RISK] fonial: 80 — Customer portal holds PII/CDR/SIP-creds/call-control; dual-backend session-confusion surface re-verified intact on a brand-new deploy (v2026.09.16-1, same-minute lockstep 4th pair verified at 10:00:17 UTC), meaning the seam survived at least two deploy cycles unchanged. Unfronted twin + live self-signup flow make the CRITICAL-severity BOLA objectively reproducible before any prod claim. Gated solely on program approval for one fabricated registration. No new passive finding this cycle; all other classes closed or out-of-scope.
