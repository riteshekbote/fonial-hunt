## 2026-09-10 23:25:16 UTC [target] (model bigpickle)
[HYP] Self-signup on prov yields usable tenants, making the two-tenant cross-bind BOLA test self-service
class: AUTH
asset: prov.fonial.de/signup/confirm/55
confidence: 60
reasoning: prov serves kundenkonto-identical Symfony fonial_user_registration (POST /signup/confirm/55, CSRF _token, AccountAddress/AccountContact/email required, trunkTariff 19|21|22) on unfronted nginx/1.10.3 with no Cloudflare; form 200 only after /signup PHPSESSID bootstrap, mirroring kundenkonto. Whether the POST creates a usable account on the prov data backend (vs order-pipeline dead-end) is unobserved.
evidence_needed: POST /signup/confirm/55 returns 2xx and the resulting account can /session/authenticate then /devices/get{"sid"} returns a real (empty) device list rather than `session unauthenticated`.
verify_steps: (HUMAN, sanctioned — ONE fake registration on prov: example.invalid email + fabricated address; admin delete-on-request.) GET /signup → jar; GET /signup/register/55 → capture _token; POST /signup/confirm/55 with all fields; then POST /api/2.0/session → SID; POST /api/2.0/session/authenticate{email,pw}; POST /api/2.0/devices/get{"sid"}. ≤1rps.
impact: Two usable test tenants without program provisioning unlocks direct proof of the 80-confidence cross-tenant BOLA (device/SIP creds + caller-ID spoof via call/initiate → CRITICAL); also exposes every kundenkonto auth/session quirk on an unfronted WAF-free host for repeatable testing. Enabler, not a standalone vuln.
testability: HUMAN_ONLY
[HYP] Cross-tenant BOLA via dual-backend SID/PHPSESSID cross-bind on unfronted prov
class: AUTH
asset: prov.fonial.de/api/2.0 (devices/get, evn/get, call/initiate)
confidence: 80
reasoning: nginx/1.10.3 direct, no Cloudflare; data endpoints authorize by body SID only, PHPSESSID decorative; /session issues cleartext UUID sid; zero drift across all probes 2026-09-09→10; gate eased this cycle by self-signup discovery (T_A/T_B obtainable without program tenant provisioning).
evidence_needed: devices/get returns tenant-B data when body sid=SID_B + Cookie:PHPSESSID_A (vs `session invalid`).
verify_steps: (HUMAN, sanctioned; T_A/T_B on prov, now plausibly self-signup-derived) 1) /session → SID_A; 2) /session/authenticate{T_A}; 3) /devices/get{"sid":SID_A} capture Set-Cookie PHPSESSID_A; 4) fresh jar /session → SID_B; 5) /evn/get{"sid":SID_B} + Cookie:PHPSESSID_A. ≤1rps.
impact: Cross-tenant CDR/device-SIP creds + call-initiate with caller-ID spoof → CRITICAL.
testability: HUMAN_ONLY
[HYP] Open self-signup on Mattermost 3.7.3 enables outsider account entry
class: BUSLOGIC
asset: mm.fonial.de/api/v4/users
confidence: 50
reasoning: system/ping 200 (X-Version-Id 3.7.x); GET /api/v4/users → 401 `session_expired`; /signup/email + /login 404 since 06:37; POST /api/v4/users untested; no passive discriminator remains (guest-token/webhook/channel reads all auth or write-gated).
evidence_needed: POST returns 201+token vs adminApproval/disabled/400.
verify_steps: (HUMAN, sanctioned) single POST /api/v4/users {email:"ot-<ts>@example.invalid",username:"ot-<ts>",password,first_name:"",last_name:""}; immediate admin delete on 201. ≤1rps.
impact: Outsider foothold in internal chat (directory, channels, webhooks) + legacy-3.7 CVE surface → HIGH if open.
testability: HUMAN_ONLY
[NEXT] HUMAN: Request program approval for ONE fake-data registration POST to https://prov.fonial.de/signup/confirm/55 (example.invalid email + fabricated address, CSRF `_token` + PHPSESSID from GET /signup; ≤1rps; SaaS admin delete-on-completion) to confirm tenant creation on the staging clone — if 2xx+authenticated `/devices/get`, proceed with the two-tenant cross-bind BOLA sequence (SID_B + PHPSESSID_A on `/evn/get`), else fall back to the single sanctioned POST to mm.fonial.de/api/v4/users.
[RISK] fonial: 48 — passive pool still exhausted, but this cycle's prov frontend/signup discovery converts the #1 hypothesis gate from "program must provision two staging tenants" to "ONE approved registration POST on an unfronted staging clone", raising reportable-outcome probability (BOLA proof self-serviceable ~60/40); MM (50) remains gated on a single approved POST; no sanctioned writes executed to date; all probes this cycle were GET-only, ≤1rps, no customer data touched.
