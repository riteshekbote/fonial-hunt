
===== TRIAGE 2026-09-07 15:35:13 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*probe*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*results*"[90m 2 matches[0m
[0m→ [0mRead probe-results.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*fonial*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*inventory*"[90m 0 matches[0m
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/fonial.md
[0m→ [0mRead leads/lead-bigpickle.md[90m [offset=437][0m
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=408][0m
[0m→ [0mRead leads/reposcan-fonial.md
After reviewing all lead files, probe results, and scope definitions, here is the 7-Question Gate validation for each distinct lead identified across the hunt:

---

## Lead 1: GraphQL 500 on `shop.fonial.de/graphql`

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `*.fonial.de` is company-owned infrastructure per scope.yml |
| Q2 Attacker reachable? | **YES** — unauthenticated, public HTTP 500 observed in probe |
| Q3 Real security impact? | **NO** — HTTP 500 alone is an error response; no data leak, no RCE, no auth bypass demonstrated |
| Q4 Provable non-invasively? | **YES** — single GET, passive |
| Q5 Novel/unreported? | Unknown; 500 is likely a schema/introspection config issue |
| Q6 Not always-rejected? | **NO** — scope excludes "Descriptive error messages"; bare 500 with no exploit chain is informational at best |
| Q7 Reasonable triager accept? | **NO** — no demonstrated impact, scanner-grade finding |

**Verdict: INVALID** — HTTP 500 without evidence of data exposure or exploitable behavior has no security impact.

---

## Lead 2: Symfony dev-mode debug pages on `dslkonto.fonial.de/app_dev.php`

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `dslkonto.fonial.de` is company-owned DSL customer portal |
| Q2 Attacker reachable? | **YES** — `app_dev.php` serves 200 pre-auth, no IP block; 404 triggers full Symfony debug page (fs path `/pkg/srv/application_2026_07_02_1410`, deploy tag, `TraceableEventDispatcher`, bundle names) |
| Q3 Real security impact? | **LOW-MED** — info disclosure (filesystem layout, deploy artifact, internal bundle naming); enables targeted exploit dev but no direct data leak or auth bypass |
| Q4 Provable non-invasively? | **YES** — single GET to `/app_dev.php/` (200) and `/app_dev.php/zzz` (404 debug page) |
| Q5 Novel/unreported? | Likely; `app_dev.php` without IP allowlist on a production customer portal is unusual |
| Q6 Not always-rejected? | **NO** — scope.yml explicitly excludes: *"Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)"*. The debug pages contain stack traces, filesystem paths, and exception semantics — exactly this class. Profiler (`/_profiler`) returns 404 (disabled). No env/params/session-data dump observed. |
| Q7 Reasonable triager accept? | **NO** — triager would close as "out of scope: descriptive error messages / stack traces" |

**Verdict: INVALID** — Content is squarely excluded by scope.yml "Descriptive error messages or headers (e.g. Stack Traces)". Standalone report value insufficient.

---

## Lead 3: Version disclosure via `X-Fonial-Version` header on `kundenkonto.fonial.de`

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `kundenkonto.fonial.de` is company-owned customer portal |
| Q2 Attacker reachable? | **YES** — header present on unauthenticated login response |
| Q3 Real security impact? | **NO** — version string (`v2026.09.01-1`) aids recon but has no direct security impact |
| Q4 Provable non-invasively? | **YES** — single GET, passive header inspection |
| Q5 Novel/unreported? | N/A — informational |
| Q6 Not always-rejected? | **NO** — scope excludes "Descriptive error messages or headers (e.g. Stack Traces, **banner grabbing**)" — version disclosure is banner grabbing |
| Q7 Reasonable triager accept? | **NO** — universally rejected as informational/out-of-scope |

**Verdict: INVALID** — Explicitly excluded by scope as "banner grabbing".

---

## Lead 4: CORS wildcard `access-control-allow-origin: *` on `kundenkonto.fonial.de` (authenticated domain)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `kundenkonto.fonial.de` is company-owned |
| Q2 Attacker reachable? | **YES** — ACAO: * confirmed on all 5 API endpoints + landing/login pages |
| Q3 Real security impact? | **NO as configured** — No `access-control-allow-credentials: true` present; browsers will NOT send cookies cross-origin. SID is in request body (not cookies). Without a demonstrated JS-readable SID leak endpoint, there is no cross-origin data exfil channel. |
| Q4 Provable non-invasively? | **PARTIALLY** — CORS headers confirmable passively; exploitability requires authenticated session + JS endpoint discovery (not yet done) |
| Q5 Novel/unreported? | N/A — misconfig is real but non-exploitable as currently configured |
| Q6 Not always-rejected? | PASS — CORS misconfig is an accepted class when exploitable |
| Q7 Reasonable triager accept? | **NO** — without `allow-credentials` + a SID leak chain, this is theoretical. Hunt bot itself parked it at confidence 35-85 with note "no JS sid-leak chain demonstrated." |

**Verdict: HOLD** — Misconfig is real and confirmed. Park until: (1) authenticated landing page is tested with valid PHPSESSID to confirm ACAO: * persists post-auth, and (2) JS bundle grep finds an API endpoint reflecting SID to cross-origin. If both confirmed → re-evaluate as HIGH. Currently non-exploitable as configured.

---

## Lead 5: Dual-backend SID/PHPSESSID session confusion (cross-tenant potential)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `kundenkonto.fonial.de/api/2.0` is company-owned API |
| Q2 Attacker reachable? | **PARTIALLY** — API is public/unauth; SID mintable via GET. But cross-tenant test requires 2 valid accounts (2FA-gated) |
| Q3 Real security impact? | **CRITICAL if confirmed** — cross-tenant CDR, SIP creds, device lists, outbound call control (toll fraud) |
| Q4 Provable non-invasively? | **NO** — requires 2 authenticated accounts for cross-bind matrix (SID-A + PHPSESSID-B). Cannot be proven passively. |
| Q5 Novel/unreported? | Yes — dual-backend (session vs data, different Content-Type + session mechanisms) is architecturally unusual |
| Q6 Not always-rejected? | PASS — AUTH/IDOR class is accepted |
| Q7 Reasonable triager accept? | **NO** — hypothesis only, confidence 75, zero proof. Triager would close as "insufficient evidence / needs PoC with two accounts." |

**Verdict: HOLD** — High-confidence hypothesis with CRITICAL impact if proven, but completely unverified. Requires 2 valid test accounts for the cross-bind matrix (SID-B body + PHPSESSID-A cookie on /devices/get, /evn/get, /call/initiate). Park until human obtains credentials and executes the test. This is the #1 priority for the next engagement phase.

---

## Lead 6: Brute-force / credential stuffing on `/session/authenticate`

| Q | Answer |
|---|--------|
| Q1-Q2 | YES / YES |
| Q3 Real security impact? | YES (credential stuffing → account takeover) |
| Q4 Provable? | YES (20 rapid requests, measure response) |
| Q5 Novel? | Unknown |
| Q6 Not always-rejected? | **NO** — scope.yml explicitly excludes: *"Enforcement policies for brute force, rate limiting, or account lockout"* |
| Q7 Reasonable triager accept? | **NO** — explicitly excluded class |

**Verdict: INVALID** — Excluded by program scope.

---

## Lead 7: DSL portal broken registration / default-product misconfiguration

| Q | Answer |
|---|--------|
| Q1 In scope? | YES |
| Q2 Attacker reachable? | YES — `/register` returns 500 pre-auth |
| Q3 Real security impact? | **NO** — "Product not found" error with no attacker-controllable parameter; DoS-flavored (registration broken), largely out of scope |
| Q6 Not always-rejected? | **NO** — "Application Denial of Service by locking user accounts" is excluded; broken registration is availability impact |
| Q7 Reasonable triager accept? | **NO** |

**Verdict: INVALID** — Availability/DoS class, out of scope.

---

## Summary

| # | Lead | Verdict | Action |
|---|------|---------|--------|
| 1 | GraphQL 500 on shop.fonial.de | **INVALID** | No impact |
| 2 | Symfony dev-mode debug pages (dslkonto) | **INVALID** | Excluded: "Descriptive error messages / Stack Traces" |
| 3 | X-Fonial-Version header disclosure | **INVALID** | Excluded: "banner grabbing" |
| 4 | CORS wildcard on auth domain | **HOLD** | Real misconfig but non-exploitable as configured; needs authenticated testing |
| 5 | Dual-backend SID/PHPSESSID cross-tenant | **HOLD** | CRITICAL if proven; needs 2-account cross-bind matrix |
| 6 | Brute-force on /session/authenticate | **INVALID** | Excluded: "Enforcement policies for brute force/rate limiting" |
| 7 | DSL broken registration | **INVALID** | DoS/availability class, excluded |

**Bottom line: 0 VALID, 5 INVALID, 2 HOLD.** No reportable finding exists yet. The sole actionable path is **Lead 5** (dual-backend cross-bind) gated behind obtaining 2 valid test accounts. Lead 4 (CORS) should be re-tested after Lead 5's human phase to check if authenticated endpoints inherit the wildcard.
