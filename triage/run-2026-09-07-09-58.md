
===== TRIAGE 2026-09-07 09:58:52 UTC =====
# TRIAGE OUTPUT — fonial GmbH Bug Bounty
# Generated: 2026-09-07
# Models producing leads: bigpickle, nemotron3 (longcat/ling3/laguna/mimo: empty stubs)

---

## LEAD 1: Dual-backend SID/PHPSESSID Cross-Binding → Cross-Tenant Access
**Source:** bigpickle (75%, HUMAN_ONLY), nemotron3 (75%, AUTH_HELPED)
**Asset:** kundenkonto.fonial.de/api/2.0 (devices/get, evn/get, call/initiate)
**Class:** AUTH

### 7-Question Gate

**Q1 In scope?** YES — kundenkonto.fonial.de is a fonial GmbH customer portal API; scope.yml covers "All company-owned infrastructure, cloud PBX systems, VoIP services, SIP platforms, APIs."

**Q2 Attacker reachable?** YES — API endpoints are public (no auth required to obtain SID from /session; unauthenticated returns `{"reason":"session unauthenticated"}`). Reachable without any credentials.

**Q3 Real security impact?** YES (if proven) — Cross-tenant CDR, SIP credentials, phone numbers, device lists, and outbound call initiation (toll fraud). CRITICAL severity.

**Q4 Provable non-invasively?** PARTIALLY — Architecture is passively confirmed: t[0m
[0m← [0mWrite triage-output.txt
Wrote file successfully.
[0m
Now I have all the data. Let me write the triage output.
**Triage complete.** Output written to `triage-output.txt`.

### Results Summary

| # | Lead | Verdict | Gate Summary |
|---|------|---------|-------------|
| 1 | **Dual-backend SID/PHPSESSID cross-binding** (kundenkonto API) | **HOLD** | Q1-Q6 pass; Q7 conditional on proof. Architecture confirmed (two independent session stores, SID in body, PHPSESSID decorative). CRITICAL impact if SID↔PHPSESSID cross-check fails. Needs 2 test accounts from different tenants to prove. |
| 2 | Symfony dev-mode debug exposure (dslkonto) | **INVALID** | Q6 fail — scope.yml excludes "Descriptive error messages or headers (Stack Traces)." Leaked content = stack traces + paths only. |
| 3 | CORS wildcard on kundenkonto | **INVALID** | Q3 low impact — no `allow-credentials`, SID in body, browsers block cross-origin cookie sends. No demonstrated SID-leak chain. |
| 4 | Brute-force / rate-limiting absence | **INVALID** | Q6 fail — scope.yml explicitly excludes "Enforcement policies for brute force, rate limiting, or account lockout." |
| 5 | X-Fonial-Version header disclosure | **INVALID** | Q6 fail — excluded class (banner/header disclosure per scope.yml). |
| 6 | DSL default-product broken registration | **INVALID** | Q3 fail — no security impact, operational issue, DoS-class. |

**Sole actionable path:** Lead 1 cross-bind matrix with 2 test accounts → report via bugs.olivermaicher.eu if proven.
:N) — Network-accessible, no privileges, no user interaction, scope changed (cross-tenant), high confidentiality + integrity impact
- **Impact:** Cross-tenant data exfiltration (CDR, SIP creds, phone numbers, device lists) + unauthorized outbound call control (toll fraud)
- **Reporting channel:** bugs.olivermaicher.eu (Oliver Maicher private program)

---

## LEAD 2: Symfony Dev-Mode Debug Exposure on dslkonto.fonial.de
**Source:** bigpickle (70), nemotron3 (90 → reclassified)
**Asset:** dslkonto.fonial.de/app_dev.php/*
**Class:** MISCONFIG

### 7-Question Gate

**Q1 In scope?** YES — dslkonto.fonial.de is a fonial GmbH customer portal (DSL); scope.yml covers all company-owned infrastructure.

**Q2 Attacker reachable?** YES — app_dev.php returns 200 on /, 200 on /forgot/request, 500 on /register, pre-auth. No IP allowlist observed.

**Q3 Real security impact?** MINIMAL — Leaked content is stack traces + filesystem paths (deploy tag `application_2026_07_02_1410`, bundle names, exception semantics). Profiler is token-gated (all 8 rotating tokens return 404). No env/param/session dump observed.

**Q4 Provable non-invasively?** YES — GET requests to /app_dev.php/* endpoints return debug pages. No auth required.

**Q5 Novel/unreported?** UNCLEAR — Leftover dev-mode entrypoint is a common misconfiguration; likely not novel.

**Q6 Not on always-rejected list?** NO — scope.yml explicitly excludes "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)." This finding is squarely in the excluded class. The stack traces, filesystem paths, and exception details are descriptive error output.

**Q7 Would a reasonable triager accept?** NO — Program scope explicitly excludes this class. Even though dev-mode is genuinely exposed, the leaked content (stack traces + paths) is listed as always-rejected. A reasonable triager would close as informational/out-of-scope.

### Verdict: INVALID

**Reason:** scope.yml OUT-OF-SCOPE clause: "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)." Leaked content = stack traces + filesystem paths only. No env/param/session data dump. Profiler disabled (404 on all tokens). Excluded class, not reportable standalone.

---

## LEAD 3: Overly Permissive CORS (Wildcard) on kundenkonto.fonial.de
**Source:** bigpickle (85 → 70), nemotron3 (85 → 80)
**Asset:** kundenkonto.fonial.de
**Class:** MISCONFIG

### 7-Question Gate

**Q1 In scope?** YES — kundenkonto.fonial.de is in-scope customer portal.

**Q2 Attacker reachable?** YES — CORS headers present on unauthenticated responses (login page, landing page, all API endpoints).

**Q3 Real security impact?** LOW as configured — `access-control-allow-origin: *` is confirmed, but NO `access-control-allow-credentials: true`. SID is transmitted in request body (not cookies). Browsers will NOT send cookies cross-origin. Direct cross-origin data theft is blocked. Without a demonstrated SID-leak chain (e.g., via Referer header, XSS, or JS reflection), the wildcard CORS is informational only.

**Q4 Provable non-invasively?** YES — CORS headers visible in response headers without auth.

**Q5 Novel/unreported?** NO — CORS wildcard on non-sensitive domains is common. The specific lack of `allow-credentials` + SID-in-body makes this non-exploitable as a standalone finding.

**Q6 Not on always-rejected list?** BORDERLINE — Not explicitly listed, but the "Clickjacking, without additional details demonstrating a specific exploit" exclusion suggests a pattern: misconfigs without demonstrated exploit chains are not accepted. The CORS wildcard without `allow-credentials` and without a SID-leak PoC falls into this category.

**Q7 Would a reasonable triager accept?** NO — The program notes say "no exposure of customer/employee/financial/auth data during testing" and the CORS wildcard alone does not demonstrate data exposure. SID in body (not cookies) + no `allow-credentials` = browsers won't send data cross-origin. Would need a chaining finding (XSS that leaks SID, Referer leak) to be reportable.

### Verdict: INVALID

**Reason:** CORS wildcard (`ACAO: *`) confirmed but non-exploitable as configured: no `access-control-allow-credentials`, SID transmitted in request body (not cookies), browsers block cross-origin cookie sends. No demonstrated SID-leak chain. Informational misconfig without actionable exploit path.

---

## LEAD 4: Brute-Force / Rate-Limiting Absence on /session/authenticate
**Source:** bigpickle (72), nemotron3 (72)
**Asset:** kundenkonto.fonial.de/api/2.0/session/authenticate
**Class:** AUTH

### 7-Question Gate

**Q1 In scope?** YES — API auth endpoint is in-scope infrastructure.

**Q2 Attacker reachable?** YES — POST endpoint returns JSON without auth.

**Q3 Real security impact?** YES (theoretical) — Credential stuffing could lead to account takeover.

**Q4 Provable non-invasively?** PARTIALLY — No rate limiting observed across 5+ rapid requests. Full proof requires 20+ requests (ACTIVE testing).

**Q5 Novel/unreported?** NO — Brute-force/rate-limiting is a well-known, commonly reported class.

**Q6 Not on always-rejected list?** YES IT IS ON REJECTED LIST — scope.yml explicitly excludes: "Enforcement policies for brute force, rate limiting, or account lockout." This is a named always-rejected class.

**Q7 Would a reasonable triager accept?** NO — Explicitly excluded by program rules.

### Verdict: INVALID

**Reason:** scope.yml OUT-OF-SCOPE clause: "Enforcement policies for brute force, rate limiting, or account lockout." Explicitly excluded class. Not reportable regardless of evidence.

---

## LEAD 5: Version Disclosure via X-Fonial-Version Header
**Source:** nemotron3 (95)
**Asset:** kundenkonto.fonial.de
**Class:** MISCONFIG

### 7-Question Gate

**Q1 In scope?** YES — In-scope customer portal.

**Q2 Attacker reachable?** YES — Header present on unauthenticated responses.

**Q3 Real security impact?** LOW — Exposes deployment version (v2026.09.01-1). Facilitates targeted exploit research but no direct data exposure.

**Q4 Provable non-invasively?** YES — GET request returns header.

**Q5 Novel/unreported?** NO — Version disclosure is extremely common and well-known.

**Q6 Not on always-rejected list?** BORDERLINE — scope.yml excludes "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)." Version headers are a form of banner/header disclosure. This falls under the excluded class.

**Q7 Would a reasonable triager accept?** NO — Information disclosure without security impact. Excluded class (banner/header disclosure).

### Verdict: INVALID

**Reason:** scope.yml OUT-OF-SCOPE: "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)." Version header = banner disclosure. Informational only, low direct impact, excluded class.

---

## LEAD 6: DSL Portal Default-Product Misconfiguration (Registration Broken)
**Source:** bigpickle (45)
**Asset:** dslkonto.fonial.de/register
**Class:** BUSLOGIC

### 7-Question Gate

**Q1 In scope?** YES — In-scope customer portal.

**Q2 Attacker reachable?** YES — /register returns 500 pre-auth.

**Q3 Real security impact?** NO — Registration is broken (500 error). No attacker-controllable parameter found. Impact = availability of public signup (DoS-flavored → largely out of scope).

**Q4 Provable non-invasively?** YES — GET request returns 500.

**Q5 Novel/unreported?** NO — Broken registration flow is common.

**Q6 Not on always-rejected list?** BORDERLINE — DoS-like (broken signup). scope.yml excludes "Application Denial of Service by locking user accounts" and network-level DoS. Broken registration is availability-related.

**Q7 Would a reasonable triager accept?** NO — No security impact. Broken registration is operational, not a security vulnerability. No exploitable parameter.

### Verdict: INVALID

**Reason:** No security impact. Broken registration flow = operational issue, not a security vulnerability. No attacker-controllable parameter found. DoS-class (out of scope). Dead endpoint.

---

## SUMMARY

| # | Lead | Verdict | Key Reason |
|---|------|---------|------------|
| 1 | Dual-backend SID/PHPSESSID cross-binding | **HOLD** | High-confidence CRITICAL flaw, needs 2 accounts to prove |
| 2 | Symfony dev-mode debug exposure | **INVALID** | Excluded class (stack traces/paths per scope.yml) |
| 3 | CORS wildcard on kundenkonto | **INVALID** | Non-exploitable as configured (no allow-credentials, SID-in-body) |
| 4 | Brute-force / rate-limiting absence | **INVALID** | Explicitly excluded per scope.yml |
| 5 | Version disclosure (X-Fonial-Version) | **INVALID** | Excluded class (banner/header disclosure per scope.yml) |
| 6 | DSL default-product misconfiguration | **INVALID** | No security impact, operational issue |

### Actionable Next Steps

**HOLD (Lead 1):** Obtain 2 test accounts on kundenkonto.fonial.de (via /signup/register/55 self-registration funnel or program-provided sandbox accounts), then execute:
1. Account A: POST /api/2.0/session → SID-A; authenticate → PHPSESSID-A
2. Account B: POST /api/2.0/session → SID-B; authenticate → PHPSESSID-B
3. POST /devices/get {"sid":"SID-B"} + Cookie: PHPSESSID=A → any data = CRITICAL
4. Repeat /evn/get, /call/initiate
5. POST /call/initiate {"sid":"SID-B", ...} + Cookie PHPSESSID=A → outbound call = CRITICAL toll fraud

Any non-`{"sid":null,"status":"error","reason":"session invalid"}` response = cross-tenant CRITICAL → report immediately via bugs.olivermaicher.eu.
